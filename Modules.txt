to create a module
cut and paste a class definition into a new file
its properties are private by default 
to make it public you have to export it

so class Person() becomes export class Person()

to import a module use

import {Person} from './person';